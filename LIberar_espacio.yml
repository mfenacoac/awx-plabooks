---
- name: Liberar espacio en disco Windows
  hosts: "{{ (target_host | default('undefined_host')).split(':')[0] }}"
  gather_facts: no
  tasks:

    - name: Mostrar variable target_host
      debug:
        msg: "target_host={{ target_host | default('NO DEFINIDO') }}"
